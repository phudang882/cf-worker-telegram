{
  "name": "telegram-bot-api-proxy",
  "version": "1.0.0",
  "description": "A Cloudflare Worker proxy for Telegram Bot API",
  "main": "worker.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Your Name",
  "license": "MIT",
  "devDependencies": {
    "@cloudflare/wrangler": "^1.19.0"
  }
}
